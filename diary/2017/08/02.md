# JavaScript Promiseの中でタイムウェイト

```
le promise = Promise.resolve();

promise.then(/* ... */)
    .then(_ => new Promise(resolve => setTimeout(() => resolve(_), 3000)))
    .then(/* ... */)
    .catch(/* ... */);
```